- name: default
  kind: postgres
  configuration:
    connection_info:
      database_url: postgres://dbuser:dbpwd@localhost:5432/appdb
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include default/tables/tables.yaml"
